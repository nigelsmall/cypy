match (writer:Entity{Id:{eId}}), writer-[:wrote]->segment-[r1:regarding_entity]->entity with entity as names, count(entity) as counts order by count(entity) desc limit 100 with names, counts match otherwriters-[:wrote]->segment-[r2:regarding_entity]->names return otherwriters.Name as Name, otherwriters.Id as Id, count(distinct names) as EntityCount order by count(distinct names) desc skip 1 limit 3;